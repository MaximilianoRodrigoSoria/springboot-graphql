type Cliente
    {
        nombre:String
        apellido:String
        email:String
    }

type Query {
    hello:String!
    suma(a:Int, b:Int):Int
    getCliente(a:Int):Cliente
    getClientes:[Cliente]
}
